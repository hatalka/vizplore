% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca_viz.R
\name{pca_viz}
\alias{pca_viz}
\title{PCA Visualization}
\usage{
pca_viz(X, y, dim = 2, center.scale = TRUE)
}
\arguments{
\item{X}{A matrix representing the input features (quantitative variables).}

\item{y}{A vector representing the categories corresponding to the input data.}

\item{dim}{Integer indicating the desired dimensionality of the visualization: 2 for 2D, 3 for 3D. Default is 2.}

\item{center.scale}{A logical (boolean) value indicating whether the data should be centered and scaled before processing.}
}
\value{
\describe{
  \item{projected_data}{The data projected onto the principal components.}
  \item{transformation_matrix}{The eigenvectors corresponding to the selected principal components.}
}
Additionally, a plotly object representing the PCA visualization is displayed.
}
\description{
Creates a 2D or 3D visualization of multidimensional data using Principal Component Analysis (PCA).
}
\examples{
# Example feature matrix
V1 <- rnorm(100, mean = 5, sd = 2)        # Normally distributed feature
V2 <- V1 + rnorm(100)                     # Correlated with V1
V3 <- runif(100, min = 0, max = 10)       # Independent feature
V4 <- V1 * 0.5 + V3 * 0.5 + rnorm(100)    # Combination of V1 and V3
# Combine into a numeric matrix
X <- cbind(V1, V2, V3, V4)
y <- sample(1:4, 100, replace = TRUE)     # Example category vector
pca_viz(X, y)                             # Default 2D visualization
pca_viz(X, y, dim = 3)                    # 3D visualization
}
